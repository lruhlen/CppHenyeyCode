#ifndef CLEANMATRIX_H
#define CLEANMATRIX_H
#include <vector>
#include <math.h>
#include <string.h>
#include "AboutThisRun.h"
#include "physicalConstants.h"
#include "global.h"
#include "printMatrix.h"
#include "matrixMultiply.h"
#include "addMatrix.h" 
#include <float.h>

using namespace std;
void cleanMatrix(struct vec3d &matrix);
void cleanMatrix(struct vec2d &matrix);
void cleanMatrix(vector<double> &matrix);
void cleanMatrix(double &matrix);

#endif
